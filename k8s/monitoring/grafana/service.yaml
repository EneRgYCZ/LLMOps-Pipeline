apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: llmops
  labels:
    app: grafana
spec:
  selector:
    app: grafana
  ports:
    - name: web
      port: 3000
      targetPort: 3000
  # Change to LoadBalancer to expose outside the cluster.
  type: ClusterIP
