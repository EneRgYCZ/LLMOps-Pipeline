apiVersion: v1
kind: Service
metadata:
  name: chromadb
  namespace: llmops
  labels:
    app: chromadb
spec:
  selector:
    app: chromadb
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
